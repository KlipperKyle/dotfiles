# ~/.ssh/config

# Send keep-alive messages occasionally, lest NAT routers forget and
# drop the connection.
ServerAliveInterval 120
ServerAliveCountMax 2

# Disable hashed known_hosts.  It is too hard to prune obsolete keys
# and too easy to lose names of previously accessed hosts.
HashKnownHosts no

# Enable the ~C command line
EnableEscapeCommandline yes

# Example rules (to make building common configs easier)

# Host bag-end bag-end.hobbiton.shire.
# 	HostName bag-end.hobbiton.shire.
# 	User bilbo
# 	Port 8022
# 	ProxyJump jumpbox.hobbiton.shire.
